{{ form_start(form) }}
    {# {{ form_widget(form) }} #}





<div class="form-row">
    
	<div class="form-group col-md-12 row">
		<div class="col-md-1">{{ form_label(form.nom, 'Catégorie') }}</div>
		<div class="col-md-3">{{ form_widget(form.nom) }}</div>
		{{ form_errors(form.nom) }}
	</div>

	<div class="form-group col-md-12 ">
		<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
	</div>
</div>
 



    
{{ form_end(form) }}
