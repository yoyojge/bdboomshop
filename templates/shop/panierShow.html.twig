{% extends 'base.html.twig' %}

{% block title %}BDboomShop{% endblock %}

{% block body %}
    




<!-- Topbar Start -->
{{ include('/shop/topBar.html.twig') }}  
<!-- Topbar End -->


<!-- Navbar Start -->
<div class="container-fluid mb-5">
    <div class="row border-top px-xl-5">
        
        {{ include('/shop/colGauche.html.twig') }}

        

        <div class="col-lg-9">

            {{ include('/shop/nav.html.twig') }}  

            {% set total = 0 %}

            {% for key, value in panier %}

                {% for product in products  %}
                    {% if product.id == key %}
                         <div>article id : {{ key }} ::{{ product.nom }}  => {{ value }} exemplaires</div>
                         <div class="mb-4">PU : {{ product.prix }} € :: TOTAL  => {{ value * product.prix }} €</div>
                         {% set totalIntermedaire = value * product.prix %}
                         {% set total = total + totalIntermedaire %}
                         
                    {% endif %}
                {% endfor %}

                 
            {% endfor %}  

            TOTAL : {{ total }}

            <div><a href="{{ path('app_shop_panierVide') }}" class="btn">Vider le panier</a></div>    
            <div><a href="#" class="btn btn-primary ">Payer avec paypal</a></div>    

           

            {# {{ dump(products) }} #}

           
            
            
    
        </table> 
            
        </div>
    </div>
</div>
<!-- Navbar End -->



















{% endblock %}
